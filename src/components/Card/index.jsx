import { Container } from "./style";

const Card = ({ name, img, type }) => {
  return (
    <Container>
      <h2>name</h2>
      <img src="#" />
      <h4>Type</h4>
    </Container>
  );
};

export default Card;
